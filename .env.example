# --- Manager ---
# Path to the jobs file (default: ./jobs.toml)
JOBS_FILE="./jobs.toml"
# Directory for generated files and the run history database (default: ./data)
DATA_DIR="./data"
# Where file-writing builtins put their output (default: DATA_DIR).
# Applies to every integration; use the per-integration variables below to
# send just one of them somewhere else.
DESTINATION_FOLDER="./data"
# Status server address; set to an empty string to disable it (default: 127.0.0.1:8787)
HTTP_ADDR="127.0.0.1:8787"
# Days of run history to keep (default: 30)
HISTORY_RETENTION_DAYS="30"
# Log level (default: info)
RUST_LOG="info"

# --- Last.fm builtins (optional) ---
# Leave LAST_FM_USERNAME unset to run vps-cron without any Last.fm jobs.
LAST_FM_API_KEY="12345"
LAST_FM_USERNAME="your_username"
# Overrides DESTINATION_FOLDER for the Last.fm exports only
# LAST_FM_DESTINATION_FOLDER="./data"
# Scrobble history database (default: DATA_DIR/scrobbles.db)
LAST_FM_DB_FILE="./data/scrobbles.db"

# --- GitHub builtins (optional) ---
# Enables github_activity. A token with no scope is enough for public data;
# the gist job additionally needs the gist scope.
GITHUB_TOKEN="ghp_xxx"
# Overrides DESTINATION_FOLDER for the GitHub exports only
# GITHUB_DESTINATION_FOLDER="./data"
# Only needed by lastfm_top_tracks_gist
GIST_ID="your_gist_id"
GIST_FILENAME="top-tracks.md"
